class ItemCarrito {
    constructor(cantidad, titulo, precio, total) {
        this.cantidad = cantidad; 
        this.titulo = titulo;
        this.precio = precio;
        this.total = total;
    }
}

export default ItemCarrito;